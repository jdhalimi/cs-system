name: cs-system
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - google-api-python-client
  - google-auth-oauthlib
  - python-dotenv
  - playwright
  - pytest
  - pytest-cov
  - mkdocs
  - pip:
      - -e .
